export const STORAGE = {
  credentials: 'caliper.jira.credentials',
  connection: 'caliper.jira.connection',
  lastIssue: 'caliper.jira.lastIssue',
} as const;

export const TOKEN_HELP_URL = 'https://id.atlassian.com/manage-profile/security/api-tokens';
